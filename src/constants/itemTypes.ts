
/**
 * Item types which are available in DB.
 */
export const itemTypes = ['track', 'tag', 'artist', 'user'];

/**
 * Item types which should be available for filtering
 */
export const filterItemTypes = ['track', 'tag', 'artist'];
